--- baseline.cu
+++ reuse_wait_64ns.cu
@@ -406,6 +406,21 @@
         : "memory"
     );
 }
+__forceinline__ __device__ void tvm_probe_reuse_wait_64ns(void* barrier, int phase) {
+    unsigned int barrier_addr_int = __cvta_generic_to_shared(barrier);
+    unsigned int ticks = 64;
+    asm volatile(
+        "{\n"
+        ".reg .pred                P1;\n"
+        "LAB_WAIT:\n"
+        "mbarrier.try_wait.parity.shared::cta.b64 P1, [%0], %1, %2;\n"
+        "@P1                       bra.uni DONE;\n"
+        "bra.uni                   LAB_WAIT;\n"
+        "DONE:\n"
+        "}\n"
+        :: "r"(barrier_addr_int), "r"(phase), "r"(ticks) : "memory");
+}
+
 extern "C" __global__ void __launch_bounds__(256) kernel_kernel(const __grid_constant__ CUtensorMap A_tensormap, const __grid_constant__ CUtensorMap B_tensormap, const __grid_constant__ CUtensorMap D_tensormap);
 extern "C" __global__ void __launch_bounds__(256) kernel_kernel(const __grid_constant__ CUtensorMap A_tensormap, const __grid_constant__ CUtensorMap B_tensormap, const __grid_constant__ CUtensorMap D_tensormap) {
   int warp_id_in_cta = __shfl_sync((uint)4294967295, (((int)threadIdx.x) >> 5), 0, 32);
@@ -492,7 +507,7 @@
         while (1) {
           if (!((_ptr_2[0] < 256))) { break; }
           for (int k = 0; k < 32; ++k) {
-            tvm_builtin_ptx_mbarrier_try_wait((&(((uint64_t*)pool_buf_ptr)[(tma_phase_stage_ptr[0] + 5)])), (tma_phase_phase_ptr[0] ^ 0));
+            tvm_probe_reuse_wait_64ns((&(((uint64_t*)pool_buf_ptr)[(tma_phase_stage_ptr[0] + 5)])), (tma_phase_phase_ptr[0] ^ 0));
             int cse_v6 = (k * 64);
             ptx_cp_async_bulk_tensor_g2s_cluster_tile_2d((&(((half*)pool_buf_ptr)[((tma_phase_stage_ptr[0] * 8192) + 512)])), (&(((uint64_t*)pool_buf_ptr)[(tma_phase_stage_ptr[0] + 1)])), ((unsigned long long)(&(A_tensormap))), 0, (uint64_t)0, (k * 64), (_ptr[0] * 128));
             ptx_cp_async_bulk_tensor_g2s_cluster_tile_2d((&(((half*)pool_buf_ptr)[((tma_phase_stage_ptr[0] * 8192) + 33280)])), (&(((uint64_t*)pool_buf_ptr)[(tma_phase_stage_ptr[0] + 1)])), ((unsigned long long)(&(B_tensormap))), 0, (uint64_t)0, (k * 64), (_ptr_1[0] * 128));
