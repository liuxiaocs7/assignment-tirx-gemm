# Blackwell GEMM：逐步优化说明与后续改进

本文对应 `d283549` 的正式内核，说明每一步解决什么开销、当前采用了哪些优化，
以及哪些方向值得继续验证。代码入口是 [gemm_kernels.py](gemm_kernels.py) 中的
`hgemm_v1`–`hgemm_v10`。所有步骤计算 **D = A @ B.T**：A 为 `(M,K)`，
B 为 `(N,K)`，FP16 输入、FP32 累加、FP16 输出。

## 1. 当前结论与验证范围

**`d283549` 的五轮全量 pytest 已确认全部通过，但性能稳定性仍需改进。**
`ea69b2c` 提交了完整文件：五轮全步骤 benchmark 中 Step 10／4096 有 2/5
超线，分别为 0.139564 / 0.139290 ms，门槛 0.139100 ms；其余形状均通过。
内核及计时源码指纹一致。完整证据见 [B300_VALIDATION.md](B300_VALIDATION.md)。
下面保留首次独立 Step 10 的七轮中位数，不能用它覆盖后续超线样本。

| Step 10 方阵尺寸 | 当前配置 | 正式 benchmark 中位数 ms | 门槛 ms |
|---|---|---:|---:|
| 1024 | N128 / EPI32 / TMEM 单缓冲 | 0.018547 | 0.032500 |
| 2048 | N128 / EPI32 / TMEM 单缓冲 | 0.027124 | 0.045500 |
| 4096 | N128 / EPI32 / TMEM 双缓冲 | 0.137610 | 0.139100 |
| 8192 | N256 / EPI64 / TMEM 单缓冲 | 0.868908 | 0.946400 |

最新五轮 pytest 的 4096 为 0.137679–0.138217 ms，均通过；五轮全步骤
benchmark 为 0.138265–0.139564 ms，只有三轮通过。首次独立七轮 benchmark
虽然 7/7 达标，最慢 0.138281 ms 的余量也仅 0.589%。当前应保留优化收益，
同时以扩大余量为下一步目标；五次运行不足以确定总体失败概率或硬件根因。

步骤编号表示引入机制的顺序，不代表任何尺寸上都越来越快。例如同一轮全量中，
4096 的 Step 5 为 0.208510 ms，Step 6 为 0.248011 ms，Step 7 为 0.217956 ms。
常驻调度和角色分工有自己的成本，收益取决于计算量、资源占用和负载分布。

## 2. 当前配置速查

这里的输出 tile 是一个 CTA 或 cluster 完成一次调度任务的输出范围；K 宽度是
输入分块的 `BLK_K`。输入 SMEM 流水线深度与 TMEM 累加器槽数是两件事。

| Step | 输出 tile | K 宽度 | 输入 SMEM 深度 | 主要机制 |
|---|---|---|---:|---|
| 1 | CTA 128×128 | 64，固定一次 | 1 | 同步加载、一次 MMA、直接写回 |
| 2 | CTA 128×128 | 64 | 1 | K 循环，结果只在最后写回 |
| 3 | CTA 128×128 | 64 | 1 | 二维 CTA 网格覆盖输出 |
| 4 | CTA 128×128 | K 是 128 的倍数时用 128，否则 64 | 1 | TMA 输入/输出、提前释放分配许可 |
| 5 | CTA 128×128 | 64 | 2 | 预取和环形复用、MMA 等待提示 |
| 6 | CTA 128×128 | 128，不能整除时回退 64 | 2 | 常驻 CTA、L2 分组调度、跨 tile phase |
| 7 | CTA 128×128 | 128，不能整除时回退 64 | 2 | TMA / MMA / 写回角色分工，EPI64 |
| 8 | CTA 128×128 | 64 | 4 | 深流水线、EPI64、缓存 TMEM 基址 |
| 9 | cluster 256×256 | 64 | 4 | 两 CTA 协作、一个 MMA consumer |
| 10 | cluster 512×128 或 512×256 | 64 | 4 | 两 consumer 共享 B，TMEM 单/双缓冲 |

Step 1–3 优先保留为可理解的教学基线。Step 4–10 的优化应保持各步的结构约束，
而不是把所有步骤替换成同一个最终内核。

## 3. 每一步怎样优化

### Step 1：先建立完整且正确的单 tile 数据通路

`hgemm_v1` 用一个 128 线程 CTA 计算固定的 128×128×64。线程协作将 A/B
加载到 SMEM，由一个选举线程发射异步 MMA，再由整个 warpgroup 将 TMEM 结果
读入寄存器，转换为 FP16 并写回。

这一阶段主要检查连续地址访问、线程到输出行的映射和 FP32 累加。必须保留
SMEM 写入后的异步代理 fence、MMA 完成等待、TMEM 读回等待及释放前的同步。
小问题的固定开销占比很高，继续加流水线或角色通常不划算；没有实测需求时不改。

### Step 2：沿 K 分块，避免中间结果往返显存

`hgemm_v2` 保留 128×128 输出 tile，将 K 分成多个 64 宽输入块。第一次 MMA
覆盖累加器，后续 MMA 累加；全部 K 块结束后只写回一次。每次完成 MMA 后推进
barrier phase，不能用上一轮的完成状态放行新一轮。

此时开销主要来自同步输入和逐 K 块的等待。下一步先让多个输出 tile 并行，
再引入异步搬运。单独优化 Step 2 时可研究连续加载和指令开销，但应保留这个
简单 K 循环作为正确性参照，尤其覆盖 K=64 的首轮语义。

### Step 3：用输出 tile 填满 GPU

`hgemm_v3` 启动 `(M/128) × (N/128)` 个 CTA，各自处理独立的输出区域。
这一步增加 GPU 并行任务数，内部仍是 Step 2 的同步 K 循环。

优化关注 tile 大小、任务数量与资源占用的平衡。更大的 tile 可以增加数据复用，
也可能减少并行任务并提高寄存器/SMEM 占用。当前先保留 128×128；引入 TMA
比在这条同步教学路径上大规模调参更符合后续步骤的目标。

### Step 4：用 TMA 搬运，并缩短串行同步链

`hgemm_v4` 用 TMA 加载 A/B 和写回 D。当前只有一份输入 SMEM，每轮仍按
“TMA 完成 → MMA 完成 → 复用输入”执行；使用异步指令本身不等于已经隐藏延迟。

已采用两项关键改动：只分配所需的 128 列 TMEM，并在唯一一次分配后立即
`relinquish_alloc_permit`；K 是 128 的倍数时使用 K128，使 K 循环中的同步
轮数减半。其他合法 K 使用 K64，保持短 K 和原有对齐要求。

在各自历史同轮对照中，2048 提前释放分配许可使中位耗时从 0.076641 降至
0.043150 ms（[记录](results_b300/step45_probe.PS9CFi/probe/summary.csv)）；
1024 使用 K128 从 0.022709 降至 0.018613 ms
（[记录](results_b300/mma64_step4.dh9ZC8/probe/summary.csv)）。
这不是把不同实验的收益直接相乘。降低 TMEM 列数本身曾收效有限，关键还在许可
的持有时机；释放许可不会释放现有 TMEM，真正 `dealloc` 必须等到写回结束。

下一步的结构性优化是 Step 5 的输入预取。此前减少读回寄存器、显式展开 K
均没有带来可采用的收益，不应只凭寄存器数更少判断更快。

### Step 5：双缓冲输入，让下一块加载与当前计算重叠

`hgemm_v5` 使用 K64、两份输入 SMEM 和一个 128 列累加器。预取数量是
`min(2, K_TILES)`，每个槽只在 MMA 读完后重新填充；静态展开一个两槽 ring，
末尾不足两块时用条件限制访问。

沿用 Step 4 的提前释放分配许可后，2048 历史对照从 0.045228 降至
0.033455 ms。另一次 1024 对照中，仅将 MMA barrier 的等待提示设为 64 ns，
从 0.016515 降至 0.014513 ms
（[记录](results_b300/wait1024.UP24Tv/probe/summary.csv)）。TMA 等待使用同样
提示没有明显收益，持续轮询反而更慢，所以没有全局修改所有等待。

64 ns 是硬件等待提示，等待失败后继续重试，绝不能理解为“超时后直接计算”。
若再改 pipeline，首先验证 K=64/192/320 的部分 ring 与 phase；下一步用常驻
CTA 复用调度和初始化，但需要接受它可能增加控制开销。

### Step 6：常驻 CTA 与 L2 友好的 tile 调度

`hgemm_v6` 启动 `SM_COUNT` 个 CTA，使用 `ClusterPersistentScheduler2D`
按 L2 分组顺序领取多个 tile，将初始化和调度成本分摊到后续任务。
TMA 的每槽 phase、MMA phase 都活在输出 tile 循环之外。即使某个 tile 的
K 块数为奇数，下一个 tile 也必须接着使用正确的 phase。

当前输入深度为 2，K128 在不能整除时回退 K64。4096 历史同轮对照为
0.316145 → 0.249217 ms，配对加速约 1.268×
（[记录](results_b300/persistent_probe.VB42kg/probe/summary.csv)）。
这是本步 K 宽度调整的收益，不说明常驻结构一定胜过 Step 5。

低优先级候选是将小输出的 CTA 数限制为 `min(SM_COUNT, tile数)`，避免无任务
CTA 的初始化；需同步检查调度器覆盖所有 tile。大尺寸则优先研究 Step 7 的
角色并行，而不是仅增加常驻 CTA 数。

### Step 7：让 TMA、MMA、写回各自推进

`hgemm_v7` 每个 CTA 有两个 warpgroup：WG1 的 warp 3 加载，warp 0 发射 MMA，
WG0 写回。输入 K128/回退 K64、深度 2；TMEM 每次读 32 列，FP16 写回按 EPI64
经过 SMEM/TMA。网格限制为实际 tile 数与 SM 数的较小者。

并行推进依赖四条握手：输入就绪、输入可复用、累加器就绪、累加器可复用。
写回必须完成全部 TMEM 读取和对应 fence，再释放累加器；之后的 TMA store
可与下一 tile 的 MMA 重叠，但 Dsmem 仍要等待 store 完成才能覆写。

4096 的 K128 历史对照为 0.309405 → 0.219035 ms，配对加速约 1.413×
（与 Step 6 同一[记录](results_b300/persistent_probe.VB42kg/probe/summary.csv)）。
继续优化应观察哪一角色在等待，再决定深度或读回粒度。EPI128 已有无收益对照，
不能把“更宽”当作默认方向。

### Step 8：加深输入流水线，减少等待和重复基址加载

`hgemm_v8` 当前是 **K64 / 深度 4 / EPI64**，另有 TMA data-ready 的 64 ns
等待提示，以及在初始化同步后缓存不可变的 TMEM 基址。当前 Step 7 是 K128
优先，因此两者已不再只是 `PIPE_DEPTH=2→4` 的单变量比较。

2048 的历史独立对照分别为：TMA 等待提示 0.029859 → 0.029245 ms
（[记录](results_b300/step810_probe.Wl6HTg/step08/summary.csv)）；基址缓存
0.029491 → 0.028849 ms
（[记录](results_b300/profile_guided.6KUfDZ/step08/summary.csv)）。
基址快照必须在分配和 CTA 同步之后，不能缓存未初始化的地址。

后续重点是 SMEM 占用、流水深度和 K 宽度的联合取舍。当前约 148480 bytes/CTA；
直接把四级输入改成 K128，按当前布局估算约 279552 bytes，超过仓库
[资源检查](tool_tests/test_narrow_n_probe.py) 使用的 B300 动态 SMEM 上限
232448 bytes/CTA。K128/深度2可以作为新组合实验，但不能
宣称等价于“直接套用 Step 7 的收益”，还需核对编译资源与全部评分形状。

### Step 9：双 CTA 协作，扩大一次 MMA 的输出范围

`hgemm_v9` 每个 cluster 有两个 CTA，形成 256×256 输出 tile。每个 CTA
加载自己的 A 行和部分 B；CTA0 发射 `cta_group=2` MMA，硬件读取两 CTA 的
操作数。保留 K64、四级输入和一个 MMA consumer。

此步的重点是跨 CTA 协议：TMA 期望字节数计入两 CTA，MMA 完成信号发给
两 CTA，累加器释放等待共 256 个读回线程。最终必须 cluster 同步后释放 TMEM。
扩大 tile 减少任务数，也增加协作成本，小尺寸不一定合算。

当前未采用 Step 8/10 的不可变 TMEM 基址缓存。可先做这个范围较小的实验，
在 cluster 同步后取快照，比较生成代码及四尺寸收益；这是待测候选，没有
Step 9 的独立加速证据。更大的结构变化由 Step 10 的两 consumer 共享 B 引入。

### Step 10：共享 B，再按工作量选择 tile 和累加器槽

`hgemm_v10` 每 CTA 三个 warpgroup：WG2 的 warp 3 加载两份 A 和一份 B，
warp 0/1 分别发射两个 consumer 的 MMA；WG0/WG1 分别写回。两个 consumer
计算不同 M 区域，共用该 K 阶段的 B。cluster 的输出高度为 512。

已采用基址缓存、B-first TMA 和均衡 cluster 网格，以及以下编译期选择：

```text
Narrow = M*N <= 4096*4096
MMA_N = 128 if Narrow else 256
EPI_N = 32 if Narrow else 64
tiles = (M//512) * (N//MMA_N)
max_clusters = SM_COUNT//2
TMEM_BUFFERS = 2 if Narrow and tiles > max_clusters else 1
tiles_per_cluster = ceil(tiles/max_clusters)
clusters = ceil(tiles/tiles_per_cluster)
```

在 148 SM 的 B300 上，1024/2048 的窄 tile 数为 16/64，一波就够，没有后续
tile 可用于双缓冲重叠。4096 为 256 个窄 tile，64 个 cluster 各处理四个；
双槽让下一 tile 有机会与前一 tile 的读回重叠。8192 保留宽 N，避免缩窄后
增加调度和交接成本。接口仍要求 M%512、N%256、K%64 均为零且尺寸为正。

| 历史四尺寸对照 | 原宽 N 中位数 ms | 窄 N / EPI32 单缓冲 ms | 窄 N 双缓冲 ms | 采用理由 |
|---|---:|---:|---:|---|
| 1024 | 0.027389 | **0.018566** | 0.018570 | 缩窄 tile 已有主要收益 |
| 2048 | 0.043305 | **0.027029** | 0.027035 | 单槽足够，耗时降低约 37.6% |
| 4096 | 0.138129 | 0.138861 | **0.137422** | 双槽才使完整组合胜过原宽 N |
| 8192 | **0.868543** | 0.909238 | 0.902522 | 双槽仍比原宽 N 慢约 3.9% |

数据来自 [四尺寸对照](B300_VALIDATION.md#历史采用依据step10_tmem_sizesi9ngij)。
4096 双缓冲在两次独立实验中累计 14/14 轮均快于各自 baseline 和直接单缓冲
对照，之后才进入正式内核；正式结果见本文开头。小尺寸的明显收益来自窄 tile，
不能归功于 TMEM 双缓冲。

双缓冲复用原有 512 列 TMEM，每个 consumer 各有两个 128 列槽。槽位索引为
`stage*2 + consumer`，地址为索引乘 128。ready/free barrier 与槽一一对应，
双槽的 phase 在两个槽轮转后翻转，单槽保留原推进位置；每槽仍等两 CTA 共
256 个读回线程，输入槽仍等两个 MMA consumer。WG0/WG1 使用不同的写回
barrier ID，不能合并。

## 4. 当前代码还值得改什么

以下是后续候选，**尚未采用到正式内核**。全量 pytest 通过与 benchmark
偶发超线同时存在，优先处理 Step 10／4096 余量；当前路径输入环实验已回传，
五级小收益在独立复测中重复出现，但仍有 3/7 超线；两级 K64/K128 明显退化，
尚无稳定修复。最新共享 A 实验数值和计时均通过，但 baseline 本身出现大幅
变化，暂不能据此采用候选。

| 优先级 | 方向 | 具体做法与采用条件 |
|---|---|---|
| 高 | 区分运行状态变化与 Step 10／4096 优化收益 | 共享 A 首轮 28/28 达标，但同一 baseline 比上次快约 19.8%、本轮漂移约 22.1%。先记录 GPU 身份和时钟/功耗状态；五级对原五级仅约 0.21% 配对收益，六级无额外收益，暂不采用。 |
| 中 | Step 10 配置规则更通用 | 当前只考虑输出面积、tile 数和 SM 数，未考虑 K 长度、长宽比。先测等面积不同形状和长/短 K，再决定是否需要更细的选择；保留当前四方阵的已测配置。 |
| 中 | 提高 Step 8 余量 | 两轮全量的 2048 约 0.0290 ms，门槛 0.0299 ms；8192 约 1.384–1.387 ms，门槛 1.4417 ms。联合检查深度、K 宽度和资源上限，避免 SMEM 超限或以单尺寸收益换其他尺寸退化。 |
| 低 | Step 9 基址缓存、Step 6 小网格 | 分别验证编译代码是否减少重复加载、空闲 CTA 是否减少；没有稳定同轮收益就维持原实现。 |

当前 **N128 双缓冲**的 `step10_current_profile.3cjntP` 报告显示 Tensor Core
活跃周期占 SM 活跃期 93.27%、占总时段 75.21%，L2/DRAM 吞吐占峰值
36.13%/9.85%。MMA 约 71.66% 在等输入，但 TMA 约 81.44% 在等输入槽释放；
这提示输入环与 MMA 之间的相互等待，不能直接说显存带宽不足。累加器复用
等待约 0.175%，暂不优先增加 TMEM 缓冲。详细计数器、trace 定义和限制见
[验证记录](B300_VALIDATION.md)。NCU 时长不评分，WarpStateStats 的 ratio
也不能解释为角色等待时间百分比。

`step10_input_ring.9SsKZM` 中 K128 两级与当前 K64 四级具有相同输入容量，
但耗时中位数增加约 20%；两级 K64 增加约 61%。这说明输入总容量并非唯一因素，
不能仅凭减少同步轮数推断提速。五级 K64 中位数为 0.138660 ms，最大值
0.138849 ms；独立复测的中位数/最大值为 0.138745/0.139429 ms，三个样本超线。
相对收益重复出现，绝对余量不足。当前布局六级动态 SMEM 达
263168 B，超过 232448 B 上限，不能直接继续加深。

共享 A probe 将 consumer 从沿 M 排列改为沿 N 排列，cluster 输出由 512×128
变为 256×256，每个 consumer 仍执行同样的 256×128 MMA。输入从 A0+A1+B
变为 A+B0+B1，每级两 CTA 的 TMA 请求从 81920 B 减到 65536 B。新布局五级
动态 SMEM 为 181248 B，六级为 214016 B，因此可在上限内继续检验六级。
它也改变 L2 访问与输出坐标，20% 字节节省不能等同于 20% 提速。
`step10_share_a.r7r3iv` 首轮验证通过，但时序漂移大于候选收益，不能据此认定
20% 输入请求节省带来了整体提速。代码仅在独立实验中启用，正式内核尚未采用；
下一步命令见 [RUNNING.md](RUNNING.md#共享-a-结果漂移时的状态采集复测)。

已有一些低收益方向可暂时搁置：四级显式展开曾生成与 baseline 完全相同的
cubin；批量 MMA 发射减少 R2UR 却没有对应提速；更浅流水线、SMEM epilogue
双缓冲、部分读回/TMA 微调没有足够收益。它们与已采用的 **TMEM 累加器双缓冲**
不是同一改动。详情保留在验证历史中，有新的编译或计数器证据时再重试。

维护方面有几项比大规模重构更实际：

| 位置 | 当前情况 | 建议改进 |
|---|---|---|
| `SM_COUNT` 的直接调用接口 | pytest 的 session fixture、benchmark 和诊断入口已设置实际 SM 数；直接 import builder 仍使用全局默认 148 | 在宿主配置层明确传入/校验设备参数，逐步减少全局状态；保持提交文件自包含，不在内核文件引入 Torch 依赖。 |
| `profile_persistent.layout` | 重复实现 Step 10 的 tile/网格选择，已有测试核对一致性 | 后续修改选择规则时同步维护；若继续扩展，抽取纯配置计算或加强边界契约，避免诊断记录与实际网格偏离。 |
| probe 的源码变换 | 使用严格匹配的字符串替换，匹配失败会拒绝继续；旧变体有基线保护 | 新实验明确记录基线/直接对照/变换版本，保留历史 builder fixture。不要放宽匹配以强套旧变体，也不必抽象十个教学内核为一个复杂模板。 |
| GPU 边界覆盖 | 正式 Step 10 已测矩形 K64/320；更广选择边界目前主要靠本地源码重放 | 若扩展适用范围，再加面积阈值两侧、单波次两侧、长 K、极端长宽比的 GPU 数值检查，保持原评分用例与计时标准。 |
| 源码注释 | Step 8 的“仅修改深度”已过时；Step 10 矩形测试注释写 96 tile，当前窄 N 实为 192 | 后续独立做注释修订，说明当前参数与教学概念的区别；本文已按现代码解释。此次不改内核文件，以保留验收源码指纹。 |
| 运行记录 | benchmark 已保存源码和原始样本，普通 pytest 日志没有自动附加相同指纹 | 每次验收附上 commit、kernel SHA、退出码及 Slurm 作业信息，保留成功和失败轮次；命令见 RUNNING。历史 results 中有测试 fixture，归档前先保留依赖清单和哈希。 |

## 5. 下一次改动怎样判断有效

1. **固定对照。** 以 `d283549` 为当前通过的基线，记录源码和编译参数。当前
   `probe_persistent.py` 默认只测生产 baseline；旧 N256 变体应在其历史提交
   重放，新输入环实验通过 `--variants` 显式选择，不能重复套用已采用变换。
2. **先验证再计时。** 检查数值、短 K、部分 ring 和跨 tile 复用，核对生成的
   CUDA/TMA 描述符及 SMEM/寄存器/溢出。源码不同但二进制相同不构成优化证据。
3. **比较同轮结果。** 保持原 CUDA-event 的 warmup=10、repeat=30，按轮交错
   对照与候选；看配对加速、原始样本和最大值，不跨 GPU 状态拼接最优成绩。
4. **覆盖全部受影响尺寸。** 4096 的收益不能外推 8192；候选有稳定收益后再
   采用，运行正式 Step 测试、benchmark 和全量测试。改同步协议需特别关注边界。
5. **分清达标、加速与诊断。** 原评分为 `time <= reference_time * 1.30`；
   benchmark 汇总 PASS 表示中位数达标，不能自动推断每轮都达标。pytest 使用
   单轮批量平均耗时。NCU、sanitizer、角色插桩的耗时只作诊断，不作评分。

当前 N128 双槽输入环实验、角色/NCU 诊断及后续验收命令见 [RUNNING.md](RUNNING.md)。
此次保留内核与原门槛，没有将离线日志核对报告为新的 GPU 性能修复。
