[project]
name = "assignment"
version = "0.1.0"
description = "Assignment trix gemm"
requires-python = ">=3.10"
dependencies = ["torch>=2.8", "pytest>=8"]

[project.optional-dependencies]
tilelang = ["tilelang==0.1.13"]

[tool.pytest.ini_options]
testpaths = ["tests"]